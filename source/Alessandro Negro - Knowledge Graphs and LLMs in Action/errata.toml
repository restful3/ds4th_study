# 『Knowledge Graphs and LLMs in Action』 (Manning, 2025) 에서 확인한 오류·불일치 정본.
#
# 노트북을 만들면서 책 코드를 실제로 실행해 얻은 것들이다. 노트북 안에만 두면 검색도
# 중복 제거도 안 되므로 여기 모은다. 각 항목의 `notebook` 이 재현·설명이 있는 곳이다.
#
# status
#   confirmed      실행·측정으로 확정했다
#   editorial      원서 PDF→md 변환이나 편집 과정의 손상 (책 내용 자체는 맞다)
#   version-drift  집필 시점과 지금의 도구·데이터·모델이 달라 생긴 차이 (오류가 아니다)
#   upstream       책이 아니라 저자 코드 저장소의 결함
#   open           불일치는 확정했으나 원인은 미확정
#
# kind
#   code       인쇄된 코드가 그대로는 동작하지 않는다
#   value      인쇄된 수치가 재현되지 않는다
#   semantics  본문 설명이 코드·데이터의 실제 동작과 다르다
#   extraction 캡션·수식이 변환 과정에서 유실·손상됐다
#
# 검증 환경 (달라지면 value 항목을 다시 확인해야 한다)
[env]
python = "3.10.18"
neo4j_server = "5.26 (Community n10s / Enterprise gds+apoc)"
neo4j_driver = "5.28.4"
torch = "2.13.0+cu130"
torch_geometric = "2.8.0"
networkx = "3.4.2"
verified_on = "2026-07-25"

# ────────────────────────────────────────────── 4장
[[erratum]]
chapter = 4
location = "Listing 4.8"
kind = "extraction"
status = "editorial"
observation = """
`gds.graph.project` 리스팅의 코드 블록은 원서 md 에 있으나 `Listing 4.8` 캡션 줄이 없다.
주석 콜아웃이 코드 줄과 뒤엉키며 사라졌다. 저장소에는 파일이 정상적으로 있다.
"""
correction = "study.toml 의 [mapping.listings.ch04] 에 4.8 을 선언해 coverage 게이트가 인식하게 했다."
notebook = "chapter_04_from_simple_networks_to_multisource_integration/src/ch04/04_chapter_guide.ipynb"

# ────────────────────────────────────────────── 5장
[[erratum]]
chapter = 5
location = "Listing 5.6 task 설명"
kind = "semantics"
status = "confirmed"
observation = """
책 5.6 은 task 를 "The same task formulation as in listing 5.1" 로 줄여 실었다.
그러나 저장소 listings/listing_7.py 의 실제 task 는 5.1(v1)의 짧은 task 가 아니라
5.4(v2)의 긴 task(관심 엔티티·관계 목록 + 약칭 안내 두 문단) 에
"which represents diary entry of Warren Weaver (often referred to as WW)" 절을 더한 것이다.
책 표기대로 v1 의 task 에 v3 의 예시만 붙이면 v2 가 이미 얻은 정규화 효과를 잃는다.
"""
correction = "저장소 listing_7.py 가 정본이다. 노트북은 세 프롬프트의 task 를 나란히 놓고 차이를 보인다."
notebook = "chapter_05_extracting_domain_specific_knowledge_from_unstructured_data/src/ch05/05_chapter_guide.ipynb"

[[erratum]]
chapter = 5
location = "src/ch05/listings/ 번호 체계"
kind = "code"
status = "upstream"
observation = """
저장소 파일이 listing_1.py ~ listing_7.py 인데 이 번호가 책 리스팅 번호와 한 칸 어긋난다.
listing_1.py 는 UBIAI 어노테이션 API 호출인데 최종 출간본에는 그 리스팅이 없다
(원서 원문 md 에 'ubiai' 가 0회). 그래서 listing_2 = 책 5.1, listing_3 = 책 5.2,
listing_5 = 책 5.4, listing_7 = 책 5.6 이다. 출력 예시 자리(listing_4·6·8)에는 파일이 없다.
게다가 listing_1.py 는 my_token = "XXX" 자리표시자라 그대로는 실행되지 않는다.
"""
correction = "study.toml 의 [mapping.listings.ch05] 이 path 로 직접 가리킨다. listing_1.py 는 어떤 책 번호에도 대응하지 않는다."
notebook = "chapter_05_extracting_domain_specific_knowledge_from_unstructured_data/src/ch05/05_chapter_guide.ipynb"

[[erratum]]
chapter = 5
location = "Listing 5.7 캡션"
kind = "extraction"
status = "editorial"
observation = """
원서 md 의 캡션 줄이 같은 문장을 두 번 담고 있다 —
"Listing 5.7 GPT output of the prompt in listing 5.6 Listing 5.7 GPT output of the prompt in listing 5.6".
그래서 book_listings() 가 뽑아 주는 제목이 이어붙은 형태가 된다. 번호 자체는 정확해
대응에는 영향이 없다.
"""
correction = """
study.toml 에 title 을 덮어쓰지 않았다 — 원문의 손상을 감추는 쪽보다 드러내는 쪽이 낫다고
보았다. 노트북 대조표가 이 사정을 출력으로 함께 알린다.
"""
notebook = "chapter_05_extracting_domain_specific_knowledge_from_unstructured_data/src/ch05/05_chapter_guide.ipynb"

[[erratum]]
chapter = 5
location = "그림 5.2"
kind = "extraction"
status = "editorial"
observation = """
원서 md 에서 그림 5.2 가 이미지 두 장으로 쪼개져 있고 그 사이에 일기 본문이 OCR 텍스트로
끼어 있다 (md L84 · L90). 캡션은 둘째 이미지 뒤에만 있다. 첫 이미지가 Person/Organization
주석 부분, 둘째가 Topic 주석 부분이다.
"""
correction = "노트북은 fig5-2a · fig5-2b 두 attachment 로 나눠 싣고 무엇이 잘렸는지 밝힌다."
notebook = "chapter_05_extracting_domain_specific_knowledge_from_unstructured_data/src/ch05/05_chapter_guide.ipynb"

# ────────────────────────────────────────────── 6장
[[erratum]]
chapter = 6
location = "importer/ingest_and_process.py QUERY_TITLES (L204-213)"
kind = "code"
status = "upstream"
severity = "high"
observation = """
직함 엔티티를 CREATE (t:Entity {name: tit}) 로만 만들고 label 속성을 넣지 않는다.
그런데 create_kg() 의 QUERY_CREATE_KG 는 label2 = kg_rel[2] 로 스키마와 대조하므로
['Person','HAS_TITLE','Title'] 규칙이 절대 발화하지 않는다.
실측(2026-07-25, 100쪽 처리): label 이 없는 Entity 214개,
RELATED_TO_ENTITY {type:"HAS_TITLE"} 214개, 그런데 Title 노드 0개, KG 층 HAS_TITLE 0개.
직함 정보는 Person.titles 속성으로만 살아남는다 (titles 가 빈 배열이 아닌 Person 132명).
그림 6.2 의 스키마와 13장의 KG_SCHEMA 가 모두 Title 을 포함하므로 영향이 두 장에 걸친다.
"""
correction = "업스트림 파일은 고치지 않는다. 노트북이 재현 질의로 0개임을 보이고 Person.titles 로 대체 조회하는 법을 싣는다."
notebook = "chapter_06_building_knowledge_graphs_with_large_language_models/src/ch06/06_chapter_guide.ipynb"

[[erratum]]
chapter = 6
location = "src/ch06/requirements.lock"
kind = "code"
status = "version-drift"
observation = """
lock 이 neo4j==4.4.11 · openai==1.12.0 을 pin 한다. 그러나 이 저장소의 .venv 는
neo4j 5.28.4 · openai 1.69.0 이고 그 위에서 importer/ingest_and_process.py 전체가
정상 완주했다 (2026-07-25, EXIT=0).
"""
correction = """
오류가 아니다. lock 은 출처 기록으로 두고 다운그레이드하지 않는다 — 내리면 이미 완성된
7개 챕터의 저장 출력과 어긋난다. 노트북 실행 환경 셀에 업스트림 pin 과 실제 검증 버전을
나란히 적었다.
"""
notebook = "chapter_06_building_knowledge_graphs_with_large_language_models/src/ch06/06_chapter_guide.ipynb"

[[erratum]]
chapter = 6
location = "Listing 6.2 (책 지면)"
kind = "code"
status = "confirmed"
observation = """
인쇄된 Cypher 가 `NOT ANY(x IN nodes(path1) WHERE x.name = "WW")` 로 되어 있는데
이 질의가 바인딩하는 변수는 `path` 다 (`MATCH path = …`). `path1` 은 어디에도 없다.
그대로 실행하면 Neo4j 가 "Variable `path1` not defined" 로 거부한다.
저장소의 `listings/8.2 … .txt` 는 `nodes(path)` 로 정확하다.
"""
correction = "저장소 .txt 가 정본이다. 노트북은 그것을 실행한다 (occupation='cyclotron' 에서 242행)."
notebook = "chapter_06_building_knowledge_graphs_with_large_language_models/src/ch06/06_chapter_guide.ipynb"

[[erratum]]
chapter = 6
location = "그림 6.4 캡션 ↔ run_gds() 투영"
kind = "semantics"
status = "confirmed"
observation = """
캡션은 "the relations TALKED_ABOUT, TALKED_WITH, WORKS_WITH, and STUDENT_OF" 로 관계
네 종을 든다. 그런데 run_gds() 의 GDS 투영 두 개(QUERY_GRAPH_PROJECTION_SELECTED ·
QUERY_PROJECTION_INFLUENCERS)는 모두 TALKED_ABOUT|TALKED_WITH|WORKS_WITH 세 종만 넣는다.
STUDENT_OF(실측 69건)는 betweenness · PageRank · Louvain 어느 값에도 기여하지 않는다.
그림이 STUDENT_OF 를 반영한 것처럼 읽으면 틀린다.
"""
correction = "노트북이 투영에 실제로 들어가는 관계 유형을 코드에서 뽑아 캡션과 나란히 보인다."
notebook = "chapter_06_building_knowledge_graphs_with_large_language_models/src/ch06/06_chapter_guide.ipynb"

[[erratum]]
chapter = 6
location = "importer 의 LIMIT 100 (build_kg 의 n_docs=100)"
kind = "semantics"
status = "upstream"
observation = """
일기는 150쪽인데 build_kg() 가 n_docs=100 으로 고정돼 있어 page_idx 2~101 만 LLM 을 태운다.
그래서 Page 노드는 150개인데 GPTProcessed 는 100개다 (실측). 캐시에는 150쪽 전부 있으므로
상한 때문이지 데이터가 없어서가 아니다.
책 본문·그림이 드는 사례 일부가 이 범위 밖이라 기본 실행으로는 재현되지 않는다 —
예: Laurence Irving(132·133·135쪽), Ernest O. Lawrence(126쪽).
게다가 이 질의가 `WHERE NOT p:GPTProcessed` 라서 **두 번 돌리면 나머지 50쪽을 처리하고**
create_kg() 가 KG 층을 다시 만든다. 즉 임포터는 멱등이 아니고 재실행하면 모든 수치가 바뀐다.
"""
correction = "노트북은 그래프가 비어 있을 때만 파이프라인을 돌리고, 재실행 시 무엇이 달라지는지 설명한다."
notebook = "chapter_06_building_knowledge_graphs_with_large_language_models/src/ch06/06_chapter_guide.ipynb"

[[erratum]]
chapter = 6
location = "src/ch06/Readme.md — 캐시 동작 설명"
kind = "semantics"
status = "upstream"
observation = """
Readme 는 캐시 덕분에 "the entire process to run even without a valid OpenAI key" 라고
적었다. 그러나 임포터는 캐시를 보기 **전에** OpenAI 클라이언트를 만든다
(process_diaries_gpt 의 첫 줄). 키가 비어 있으면 그 지점에서 OpenAIError 로 죽는다.
캐시가 100% 적중해도 마찬가지다.
"""
correction = "업스트림은 고치지 않는다. 노트북이 실행 전에 자리표시자 값을 주입하고 그 이유를 밝힌다."
notebook = "chapter_06_building_knowledge_graphs_with_large_language_models/src/ch06/06_chapter_guide.ipynb"

[[erratum]]
chapter = 6
location = "그림 6.3"
kind = "extraction"
status = "editorial"
observation = """
원서 md L114 의 이미지에 캡션 줄이 붙지 않았다 (6.2 다음이 6.4 로 건너뛴다).
본문 L112 가 "the concrete case shown in figure 6.3" 이라 부르고,
캡션 "Figure 6.3 Graph-based entity resolution example" 은 이미지 오른쪽 안에 인쇄돼 있다.
"""
correction = "노트북은 이 이미지를 fig6-3 으로 싣는다."
notebook = "chapter_06_building_knowledge_graphs_with_large_language_models/src/ch06/06_chapter_guide.ipynb"

# ────────────────────────────────────────────── 9장
[[erratum]]
chapter = 9
location = "Listing 9.1"
kind = "code"
status = "confirmed"
observation = "함수 호출이 정의보다 앞에 인쇄돼 있어 인쇄 순서대로 실행하면 NameError 다."
correction = "저장소 파일의 정의 → 호출 순서가 맞다."
notebook = "chapter_09_machine_learning_on_knowledge_graphs_a_primer_approach/src/ch09/09_chapter_guide.ipynb"

[[erratum]]
chapter = 9
location = "Listing 9.3 주석"
kind = "semantics"
status = "confirmed"
observation = """
"a random seed ensures reproducible results" 라고 적었지만 같은 리스팅이 workers=4 를 쓴다.
gensim Word2Vec 이 네 스레드로 학습해 가중치 갱신 순서가 스케줄링에 좌우된다.
임베딩 행렬을 비트 비교해 workers=1 은 재현, workers=4 는 미재현을 실측했다.
책 9.7 둘째 블록(F1 0.6447)이 재현되지 않는 진짜 이유다.
"""
correction = "재현이 필요하면 workers=1 로 둔다. 책도 F1 이 30~70% 를 널뛴다고 적었다."
notebook = "chapter_09_machine_learning_on_knowledge_graphs_a_primer_approach/src/ch09/09_chapter_guide.ipynb"

[[erratum]]
chapter = 9
location = "Listing 9.5"
kind = "extraction"
status = "editorial"
observation = "캡션 줄이 원서 md 에 없다. 9.4 와 9.6 사이의 EvaluateEmbedding.evaluate 블록이 그것이다."
correction = "study.toml 에 9.5 를 선언했다 (symbol = EvaluateEmbedding.evaluate)."
notebook = "chapter_09_machine_learning_on_knowledge_graphs_a_primer_approach/src/ch09/09_chapter_guide.ipynb"

[[erratum]]
chapter = 9
location = "Listing 9.8"
kind = "extraction"
status = "editorial"
observation = "`degree =` 의 우변이 소실되고 주석 조각이 끼어들었다. 저장소 코드가 정본이다."
notebook = "chapter_09_machine_learning_on_knowledge_graphs_a_primer_approach/src/ch09/09_chapter_guide.ipynb"

[[erratum]]
chapter = 9
location = "src/ch09/requirements.lock"
kind = "code"
status = "upstream"
observation = "scikit-learn 과 pandas 가 빠져 있어 깨끗한 환경에서 tasks.py init 만 하면 ModuleNotFoundError 다."
correction = "원본은 고치지 않고 노트북 실행 환경 셀에 표로 경고했다."
notebook = "chapter_09_machine_learning_on_knowledge_graphs_a_primer_approach/src/ch09/09_chapter_guide.ipynb"

# ────────────────────────────────────────────── 10장
[[erratum]]
chapter = 10
location = "Listing 10.15 (Cypher)"
kind = "code"
status = "confirmed"
severity = "high"
observation = """
DWPC 차수 계산의 첫 항이 바인딩되지 않은 변수 v 를 쓴다 — count{(v)-[:BINDS_CbG]-()}.
패턴이 바인딩하는 것은 c·g·d 뿐이다. 10.16·10.17 은 같은 자리에 c 를 쓴다 (프로그램으로 확인).
hetionet 실측: count{(c)-...} = 56 (Metformin 차수) 대 count{(v)-...} = 23,142 (전역 합계).
PDP 가 (23142/56)^-0.4 = 0.0898 배로 눌려 DWPC 가 0.0007028 이 된다. PC 는 영향 없다.
"""
correction = "v -> c 로 고치면 DWPC 0.0078220 (11.1배). 책 본문이 인용하는 0.0007 자체가 이 버그의 산물이다."
extra = "src/ch10/listings/12.18 의 LLM 프롬프트가 이 쿼리를 '검증된 예시' 로 심어 놓았다."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

[[erratum]]
chapter = 10
location = "표 10.4 ↔ Listing 10.6"
kind = "semantics"
status = "confirmed"
observation = """
둘이 다른 양을 센다. 표는 인접행렬 A^k 의 사기꾼 열 합(= 길이 k 걸음 수, 목표 집합에 자기 포함),
리스팅은 "최단 거리가 k 인 사기꾼 수" 다. 걸음 수로 재구성하면 20/20 일치, 리스팅 정의로는 6/20.
리스팅 값은 사기꾼 4명을 넘을 수 없는데 표의 A 행은 1+4+18=23 이다. 리스팅은 사기꾼 노드의
홉 수를 0 으로 비우는데 표에는 값이 있다.
"""
correction = "노트북은 두 정의를 나란히 계산해 차이를 보이고, 어느 쪽이 옳다고 판정하지 않는다."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

[[erratum]]
chapter = 10
location = "표 10.6 ↔ Listing 10.8"
kind = "value"
status = "confirmed"
observation = "표는 정규화하지 않은 매개 중심성인데 리스팅 기본 인자는 normalized=True 다. normalized=False 로 20/20 일치, 기본값으로는 9/20(0 인 값만 우연히 맞는다). 정규화 상수 C(19,2)=171."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

[[erratum]]
chapter = 10
location = "표 10.7 사기 가중 PageRank"
kind = "value"
status = "confirmed"
observation = "리스팅 기본 개인화(fraud_weight=2)로 재현되지 않는다. 후보 5개를 역추적해 '사기꾼만 1, 나머지 0' 이 정답임을 확정했다 (L1 오차 0.009 대 기본값 0.575)."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

[[erratum]]
chapter = 10
location = "표 10.7 노드 Q 기본 PageRank"
kind = "value"
status = "confirmed"
observation = "0.75 로 인쇄됐지만 실제는 0.076 이다. 0.75 로 두면 20개 값 합이 1.663 이 되어 확률분포가 아니다. 자리 하나가 밀린 오타다."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

[[erratum]]
chapter = 10
location = "그림 10.10a 의 합 수식"
kind = "extraction"
status = "editorial"
observation = "원문 md 가 `2 + 4 + 4 + 3 + 1 + 9 = 17` 로 옮겼는데 좌변 합이 23 이다. 그림 안 인쇄값 `2(H)+4(I)+3(B)+4(G)+3(O)+1(T)=17` 이 정답이고 실제 차수와 맞는다. 해설판이 깨진 식을 그대로 옮기며 'O=9' 라고 적었으나 O 의 차수는 3 이다."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

[[erratum]]
chapter = 10
location = "Listing 10.5"
kind = "extraction"
status = "editorial"
observation = "코드가 텍스트가 아니라 스캔 이미지로 실려(원문 L237·L239) book_listings() 정규식이 잡지 못한다."
correction = "study.toml 에 10.5 를 선언했다."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

[[erratum]]
chapter = 10
location = "해설판 10.3.2 연습문제"
kind = "semantics"
status = "editorial"
observation = "'저장소에 Neo4j 의 Hetionet 에 연결하는 전체 코드가 있다' 고 하지만 없다 (12.19 는 create_fraud_network() 만 쓴다)."
correction = "노트북 실습 10-D 가 그 연결을 직접 만든다 (Cypher -> networkx -> ReFeX, 노드 946 / 간선 1,655)."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

[[erratum]]
chapter = 10
location = "src/ch10/listings/12.10·12.11·12.12"
kind = "code"
status = "upstream"
observation = "임포트는 되지만 호출하면 NameError 다. 실행 가능한 판은 12.13 하나뿐이다."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

[[erratum]]
chapter = 10
location = "src/ch10/listings/12.14"
kind = "code"
status = "upstream"
observation = "`import numpy as np` 가 빠져 operator_l1/operator_l2 가 깨진다. catenate 는 list, 나머지는 ndarray 를 요구해 입력 타입이 섞인다."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

[[erratum]]
chapter = 10
location = "src/ch10/listings/12.19 (ReFeX)"
kind = "code"
status = "upstream"
observation = "반복마다 원본 열에도 .sum/.mean 을 다시 붙여 같은 이름·같은 값의 열 12쌍이 생긴다 (54개 중 고유 42개). 가지치기를 끄면 df[name] 이 Series 를 돌려줘 조용히 깨진다. 책 10.18 과 달리 가지치기를 반복마다 수행해 결과 열 집합도 다르다."
notebook = "chapter_10_graph_feature_engineering_manual_and_semiautomated_approaches/src/ch10/10_chapter_guide.ipynb"

# ────────────────────────────────────────────── 11장
[[erratum]]
chapter = 11
location = "Listing 11.2"
kind = "code"
status = "confirmed"
observation = "`self.head_dim = input_dim` 이지만 torch.chunk 는 input_dim // num_heads 크기로 쪼개므로 헤드 차원이 어긋난다. 또 이 리스팅이 쓰는 GraphAttentionHead 의 정의가 책에 없다 (저장소 L8-34 에만 있다) — 그대로 옮기면 NameError 다."
correction = "저장소는 input_dim // num_heads + divisibility assert 로 고쳤다."
notebook = "chapter_11_graph_representation_learning_and_graph_neural_networks/src/ch11/11_chapter_guide.ipynb"

[[erratum]]
chapter = 11
location = "Listing 11.3"
kind = "code"
status = "confirmed"
observation = "Q [batch,nodes,dim] 와 K [batch,nodes,neighbors,dim] 를 그대로 matmul 해 shape 이 맞지 않는다."
correction = "저장소는 node_features.unsqueeze(2) 로 이웃 축에 브로드캐스트한다."
notebook = "chapter_11_graph_representation_learning_and_graph_neural_networks/src/ch11/11_chapter_guide.ipynb"

[[erratum]]
chapter = 11
location = "Listing 11.4"
kind = "code"
status = "confirmed"
observation = "정의되지 않은 전역 max_degree 를 참조해 NameError 다."
correction = "저장소는 max_degree 를 생성자 인자로 받고 nn.Embedding(max_degree + 1, ...) 로 여유를 둔다."
notebook = "chapter_11_graph_representation_learning_and_graph_neural_networks/src/ch11/11_chapter_guide.ipynb"

[[erratum]]
chapter = 11
location = "Listing 11.5 · 11.6"
kind = "code"
status = "confirmed"
observation = "torch.cat(..., dim=1) 을 쓴다. [batch, nodes, features] 에서 dim=1 은 노드 축이라 잘못이다. 노트북이 재현한 오류: RuntimeError: mat1 and mat2 shapes cannot be multiplied (40x64 and 128x64)."
correction = "dim=-1 이 맞다 (저장소가 그렇게 한다)."
notebook = "chapter_11_graph_representation_learning_and_graph_neural_networks/src/ch11/11_chapter_guide.ipynb"

[[erratum]]
chapter = 11
location = "src/ch11/listings/GNN_all_in_one.py (FlexibleGNN)"
kind = "code"
status = "upstream"
observation = """
책에 없는 통합 예제다. 결함 둘. (1) architecture='gat' 은 순전파에서 TypeError — forward 의
else 분기가 인자 셋을 넘기는데 MultiHeadGraphAttention.forward 는 둘만 받는다 (책 11.2 자체는
정상이다). (2) 층마다 최초 neighbor_features 를 다시 집계해 num_layers 를 늘려도 진짜 k-홉
전파가 아니다.
"""
correction = "노트북이 '교육용 조립대' 로 읽어야 한다고 명시한다."
notebook = "chapter_11_graph_representation_learning_and_graph_neural_networks/src/ch11/11_chapter_guide.ipynb"

# ────────────────────────────────────────────── 12장
[[erratum]]
chapter = 12
location = "12.1.2 절 본문 (라벨 순서)"
kind = "semantics"
status = "confirmed"
severity = "high"
observation = """
본문은 라벨 0 이 합법(licit)이라고 적었다. 그런데 LabelEncoder 는 ['1','2','unknown'] 을
사전순 정렬하므로 0 = illicit 이다. 표 12.1(불법=원본 1)과 혼동 행렬 그림(첫 행이 illicit)도
0 = 불법을 가리킨다. 이 오류를 그대로 믿으면 혼동 행렬 해석이 정반대로 뒤집힌다.
"""
correction = "노트북이 le.classes_ 실측으로 바로잡는다."
notebook = "chapter_12_node_classification_and_link_prediction_with_gnns/src/ch12/12_chapter_guide.ipynb"

[[erratum]]
chapter = 12
location = "Listing 12.21"
kind = "code"
status = "confirmed"
observation = """
LinkNeighborLoader 에 분할 전 전체 그래프(data=data)를 넘긴다. 검증·테스트용으로 떼어 둔
엣지가 학습 중 메시지 전달에 쓰여, 12.2.3 절이 disjoint_train_ratio 로 막은 누출이 더 큰
규모로 되돌아온다. PyG 공식 이질 링크 예측 예제는 data=train_data 를 쓴다.
"""
correction = "노트북 기본값은 train_data 이고 LEAKY_BOOK_LOADER = True 로 책 재현이 가능하다."
notebook = "chapter_12_node_classification_and_link_prediction_with_gnns/src/ch12/12_chapter_guide.ipynb"

[[erratum]]
chapter = 12
location = "Listing 12.21 + RandomLinkSplit 조합 (평가 라벨 손상)"
kind = "code"
status = "confirmed"
severity = "high"
observation = """
검증·테스트 로더의 라벨이 3개 클래스로 오염된다. `RandomLinkSplit(neg_sampling_ratio=2)`
가 이미 `edge_label` 에 음성(0)을 넣어 두는데, 리스팅 12.21 형태로 `LinkNeighborLoader` 에
`neg_sampling_ratio=2` 를 **다시** 넘기면 PyG(`link_loader.py`)가 `edge_label.min() == 0`
을 보고 `edge_label + 1` 로 밀어 올린다. 원래 음성 0->1, 양성 1->2, 새 음성이 0 이 된다.
실측 라벨 분포 90,747건 = {0: 60,498, 1: 20,166, 2: 10,083}.

그래서 이 채점표로는 **완벽한 이진 예측기도 가중 F1 0.5776 이 상한** 이고 "전부 링크 없음"
이 0.5333 이다. 관측된 0.5691/0.5374/0.5672 는 상한의 98.5%/93.0%/98.2% 다 — 모델은
거의 최적으로 동작했고 깨진 것은 채점이었다.
"""
correction = """
학습용 배치는 정상(1:2 이진)이고 평가 경로만 깨진다. 로더에 넘기는 음성 비율을 빼거나
분할 단계의 음성을 쓰지 않아야 한다. 노트북 「원인 후보 검증」 절이 정정 로더로 재채점한다.

이 오염이 앞선 두 오판을 낳았다.
  * "책 0.93 대비 크게 미달" — 서로 다른 채점표라 비교가 성립하지 않는다
  * "거짓 음성률 82~98%" — 3x3 혼동 행렬을 2x2 로 오독한 값이다 (실제로는 원래 음성의
    참 음성률). eval/eval_funcs.py 의 generate_confusion_matrices_batched 가 3x3 을 낸다
  * "임베딩 암기" 가설의 근거였던 "학습 F1 0.90 대 검증 0.57" 도 서로 다른 채점표다
정보 누출 가설 기각은 유효하다 — 누출판·비누출판이 같은 채점표를 썼으므로 서로 간 비교는
성립한다.
"""
notebook = "chapter_12_node_classification_and_link_prediction_with_gnns/src/ch12/12_chapter_guide.ipynb"

[[erratum]]
chapter = 12
location = "12.2 절 링크 예측 성능 (책 수치 재현)"
kind = "value"
status = "open"
observation = """
위 라벨 오염을 바로잡은 뒤에도 책이 보고한 약 0.93/0.91/0.93 에 이르는지는 확인하지 못했다.
정정 로더로 재채점한 값은 노트북 「원인 후보 검증」 절에 있다.
"""
correction = "라벨 오염이 확정됐으므로 이 항목은 '깨진 채점표를 고친 뒤 남는 격차' 만 가리킨다."
notebook = "chapter_12_node_classification_and_link_prediction_with_gnns/src/ch12/12_chapter_guide.ipynb"

[[erratum]]
chapter = 12
location = "해설판 그림 12.6 · 12.11 캡션"
kind = "extraction"
status = "editorial"
observation = "해설판이 세 조각을 '(1/3) GCN · (2/3) GAT · (3/3) SAGE' 로 적었지만 원서 원문 등장 순서는 SAGE -> GCN -> GAT 다 (각 이미지의 제목 텍스트로 확인)."
correction = "노트북은 모델 순서(GCN -> GAT -> SAGE)로 재정렬해 실었다."
notebook = "chapter_12_node_classification_and_link_prediction_with_gnns/src/ch12/12_chapter_guide.ipynb"

# ────────────────────────────────────────────── 13장
[[erratum]]
chapter = 13
location = "Listing 13.5 예시 호출"
kind = "value"
status = "confirmed"
observation = """
책이 인쇄한 예시는 kg_doc_selector("August Krogh", "Person", "Lawrence Irving", "Person",
"TALKED_ABOUT") 인데 그대로 호출하면 0건이 나온다 (실측). 원인은 두 겹이다.

1) 철자. 일기 원문의 표기는 "Laurence Irving"(132·133·135쪽)이고 "Lawrence Irving" 은
   150쪽 어디에도 없다. 책 6장 본문이 이 사정을 스스로 밝힌다 — 같은 쪽에 Ernest
   Lawrence 가 있어 GPT 가 이름을 Lawrence 로 잘못 옮겼다는 것이다. 그런데 저장소에
   동봉된 응답 캐시 156개는 세 쪽 모두 "Laurence Irving" 으로 **정확히** 담고 있어
   책이 말한 그 오식을 재현하지 않는다.
2) 처리 범위. 그 세 쪽은 page_idx 132·133·135 라 임포터의 LIMIT 100(page_idx 2~101)
   **밖** 이다. 그래서 어느 철자로 부르든 기본 실행으로 만든 KG 에는 그 인물이 없다.

즉 책의 예시는 100쪽 넘게 처리하고 + 지금 캐시에는 없는 오식이 일어난 KG 를 전제한다.
"""
correction = """
기본 실행 범위 안에서 같은 메커니즘을 보이려면 "Irving" 으로 부른다 — 1건(1939-05-08
August Krogh 면담 페이지)이 나오고 내용이 책 표 13.2 의 답과 부합한다. 노트북은 책
그대로의 호출과 정정 호출을 나란히 실행해 보인다. 6장의 "임포터 LIMIT 100" 항목을 함께 보라.
"""
notebook = "chapter_13_knowledge_graph_powered_retrieval_augmented_generation/src/ch13/13_chapter_guide.ipynb"

[[erratum]]
chapter = 13
location = "code/definitions.py KG_SCHEMA · REDiarySelectorInput"
kind = "semantics"
status = "upstream"
observation = """
KG_SCHEMA 가 LLM 에게 (:Person)-[:HAS_TITLE]->(:Title) 을 알려 주고 REDiarySelectorInput 이
Title 을 대상 클래스로, HAS_TITLE 을 관계 선택지로 광고한다. 그러나 6장 임포터의 버그로
Title 노드가 0개라 그 조합은 항상 빈 결과다. 게다가 KG_SCHEMA 의 Nodes 절에는 (:Title …)
노드 정의 자체가 없어서 스키마 자체가 불완전하다.
"""
correction = "6장 QUERY_TITLES 항목을 함께 보라. 노트북은 이 조합이 왜 비는지 재현 질의로 보인다."
notebook = "chapter_13_knowledge_graph_powered_retrieval_augmented_generation/src/ch13/13_chapter_guide.ipynb"

[[erratum]]
chapter = 13
location = "Listing 13.4 · 표 13.1 유사도 점수"
kind = "value"
status = "version-drift"
observation = """
책은 "How is Lauritsen related to cyclotron research?" 에 대해 코사인 유사도 0.906 / 0.903 /
0.902 를 싣는다. 로컬 프록시에 /v1/embeddings 가 없어(HTTP 404) OpenAI 임베딩을 쓸 수 없고,
Ollama nomic-embed-text(768차원, 책은 1536차원 계열)로 대체해 측정하면 0.8169 / 0.8135 /
0.8123 이 나온다. 책이 1위로 실은 Lark-Horovitz 문서가 3위로 내려온다.
"""
correction = """
오류가 아니다. 임베딩 모델이 다르면 코사인 점수의 스케일이 달라 절대값 비교가 성립하지
않는다. 비교해야 할 것은 점수가 아니라 상위 문서 집합과 순위다.
"""
notebook = "chapter_13_knowledge_graph_powered_retrieval_augmented_generation/src/ch13/13_chapter_guide.ipynb"

[[erratum]]
chapter = 13
location = "Listing 13.6 에이전트의 도구 사용"
kind = "value"
status = "version-drift"
observation = """
그림 13.5 는 에이전트가 KG 리더 → 문서 선택기를 차례로 쓰는 흐름을 보인다. 프록시 모델로
재현하면 도구 사용이 비결정적이다 — 같은 질문·같은 모델(gpt-5.4)로 steps=0(도구 없이 즉답)과
steps=5(max_iterations 한계로 중단)가 모두 관측됐다. gpt-5.4-mini 는 같은 질문에 steps=3 이었다.
표 13.2 의 "astronomy and climatology" 대신 "biology, chemistry, physics" 가 나온다.
"""
correction = """
오류가 아니다. 프롬프트가 "Respond directly if appropriate" 를 허용하고, 최신 모델은
파라메트릭 지식만으로 답해 버리기도 한다. 노트북은 이 수치들을 단일값이 아니라 관측 범위로 적는다.
"""
notebook = "chapter_13_knowledge_graph_powered_retrieval_augmented_generation/src/ch13/13_chapter_guide.ipynb"

[[erratum]]
chapter = 13
location = "src/ch13/ 의존성 선언 부재 · cwd 상대경로"
kind = "code"
status = "upstream"
observation = """
이 챕터에는 requirements.txt 도 requirements.lock 도 Makefile 도 없어 필요한 패키지를
임포트문으로 판정해야 한다. 게다가 code/tools.py 는 load_dotenv("../.env") 를,
listings/15.6 은 load_custom_prompt("../prompts/prompt_structured.txt") 를 쓰는데 둘 다
cwd 상대경로라 src/ch13/listings/ 에서 실행할 때만 맞는다.
또 업스트림 임포트가 구버전 LangChain 을 전제한다 — langchain 1.x 는
langchain.agents.create_structured_chat_agent 와 langchain_community.graphs.Neo4jGraph 를
각각 langchain-classic · langchain-neo4j 로 옮겨 그대로는 ImportError 다.
"""
correction = """
업스트림 파일은 고치지 않는다. langchain 0.3.x 를 설치해 검증했고(0.3.30 계열),
노트북이 실행 시점에만 cwd 를 listings/ 로 옮긴다. 검증 버전은 노트북 실행 환경 셀에 있다.
"""
notebook = "chapter_13_knowledge_graph_powered_retrieval_augmented_generation/src/ch13/13_chapter_guide.ipynb"

# ────────────────────────────────────────────── 2장
[[erratum]]
chapter = 2
location = "Listing 2.1 · 그림 2.9"
kind = "value"
status = "version-drift"
observation = """
책은 Claude 3.5 Sonnet 이 농부·양 문제에서 늑대·양배추 퍼즐 풀이로 미끄러진다고 보고한다.
2026-07-25 재측정에서 gpt-5.4-mini·gpt-5.4 둘 다 정답('1회')을 냈다. 저자들도 "출간 무렵
같은 실험은 다른 결과를 낼 것이 거의 확실하다" 고 예고했다.
"""
correction = """
오류가 아니다. 다만 이 한 사례가 통과한 것이 이 절의 논지를 뒤집지는 않는다 — 근거는 Wu 등의
11개 과제 결과이고, 이 예시가 출간 후 학습 데이터에 들어갔을 가능성도 배제할 수 없다.
"""
notebook = "chapter_02_intelligent_systems_a_hybrid_approach/src/ch02/02_chapter_guide.ipynb"
