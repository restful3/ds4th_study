# ============================================================================
# LangChain AI Agent 마스터 교안 - Python 의존성
# ============================================================================
#
# 설치 방법:
#   pip install -r requirements.txt
#
# 또는 uv 사용 (권장):
#   uv pip install -r requirements.txt
#
# ============================================================================

# ============================================================================
# 핵심 LangChain 패키지
# ============================================================================

# LangChain 코어 (필수)
langchain>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0

# LangSmith (트레이싱 및 평가)
langsmith>=0.1.0

# ============================================================================
# LLM 프로바이더 (하나 이상 필수)
# ============================================================================

# OpenAI (GPT-4, GPT-4o, GPT-4o-mini)
langchain-openai>=0.2.0

# Anthropic (Claude 3.5 Sonnet, Claude 3 Opus)
langchain-anthropic>=0.2.0

# Google (Gemini Pro, Gemini 1.5)
langchain-google-genai>=2.0.0

# ============================================================================
# 도구 및 통합
# ============================================================================

# Vector Stores
langchain-chroma>=0.1.0  # Chroma (로컬 Vector DB)
# langchain-pinecone>=0.2.0  # Pinecone (관리형 Vector DB) - 선택 사항
# langchain-weaviate>=0.1.0  # Weaviate (오픈소스 Vector DB) - 선택 사항

# Document Loaders
langchain-community>=0.3.0  # 커뮤니티 통합 (PDF, TXT 등)
pypdf>=4.0.0  # PDF 파싱
python-docx>=1.1.0  # DOCX 파싱
unstructured>=0.15.0  # 다양한 문서 형식 지원

# Embeddings
sentence-transformers>=3.0.0  # 로컬 임베딩 모델

# ============================================================================
# 데이터베이스 및 스토리지
# ============================================================================

# PostgreSQL (메모리 시스템용)
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# SQLite (기본 체크포인터)
# (Python 표준 라이브러리에 포함)

# ============================================================================
# 유틸리티
# ============================================================================

# 환경변수 관리
python-dotenv>=1.0.0

# HTTP 요청
requests>=2.32.0
httpx>=0.27.0

# JSON 처리
pydantic>=2.9.0

# 타입 체크
typing-extensions>=4.12.0

# ============================================================================
# 프로덕션 도구 (Part 9-10)
# ============================================================================

# API 서버
fastapi>=0.115.0
uvicorn>=0.32.0

# 테스트
pytest>=8.3.0
pytest-asyncio>=0.24.0

# 모니터링
langfuse>=2.0.0  # 관측성 플랫폼 (선택 사항)

# ============================================================================
# 프론트엔드 (Part 9: 스트리밍)
# ============================================================================

# CORS 지원
fastapi-cors>=0.0.6

# WebSocket
websockets>=13.0

# ============================================================================
# 개발 도구 (선택 사항)
# ============================================================================

# 코드 포매팅
# black>=24.0.0
# isort>=5.13.0

# 린팅
# ruff>=0.7.0

# 타입 체크
# mypy>=1.13.0

# Jupyter Notebook
# jupyter>=1.1.0
# ipykernel>=6.29.0

# ============================================================================
# 버전 호환성 노트
# ============================================================================
#
# - Python 3.10 이상 필요
# - LangChain 1.0 기반 (2025년 1월 기준)
# - 일부 패키지는 선택 사항 (주석 처리됨)
# - 프로덕션 배포 시 버전을 고정하는 것을 권장
#
# ============================================================================
