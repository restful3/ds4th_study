# Part 2: Fundamentals - LangChain 기본 요소

> 📚 **학습 시간**: 약 2-3시간
> 🎯 **난이도**: ⭐⭐☆☆☆ (초급)
> 📖 **공식 문서**: [07-models.md](/official/07-models.md), [08-messages.md](/official/08-messages.md), [09-tools.md](/official/09-tools.md)
> 📄 **교안 문서**: [part02_fundamentals.md](/docs/part02_fundamentals.md)

---

## 📋 학습 목표

이 파트를 완료하면 다음을 할 수 있습니다:

- [x] Chat Models의 개념과 사용법 이해
- [x] 다양한 메시지 타입 활용
- [x] 기본 도구(Tool) 정의 및 사용
- [x] 고급 도구 패턴 구현
- [x] Tool Calling 메커니즘 이해

---

## 📚 개요

이 파트에서는 LangChain의 핵심 구성 요소인 **Models**, **Messages**, **Tools**를 학습합니다. 이는 Agent를 구축하기 위한 필수 기초입니다.

**왜 중요한가?**
- Chat Models은 LLM과 상호작용하는 표준 인터페이스입니다
- Messages는 대화 컨텍스트를 구조화합니다
- Tools는 Agent가 외부 세계와 상호작용하는 방법입니다

**실무 활용 사례**
- 챗봇 및 대화형 AI 시스템
- API 연동 Agent
- 데이터 조회 및 변환 시스템

---

## 📁 예제 파일

### 01_chat_models.py
**난이도**: ⭐⭐☆☆☆ | **예상 시간**: 30분

Chat Models의 기본 개념과 다양한 LLM 프로바이더 사용법을 학습합니다.

**학습 내용**:
- `init_chat_model()` 사용법
- OpenAI, Anthropic, Google 등 프로바이더 설정
- 모델 파라미터 조정 (temperature, max_tokens 등)
- Streaming 응답 처리
- Multimodal 입력 (이미지, 오디오)

**실행 방법**:
```bash
python 01_chat_models.py
```

**주요 개념**:
- Chat Model 초기화
- 동기/비동기 호출
- 응답 형식 이해

---

### 02_messages.py
**난이도**: ⭐⭐☆☆☆ | **예상 시간**: 25분

LangChain의 메시지 타입과 구조를 이해합니다.

**학습 내용**:
- `SystemMessage` - 시스템 프롬프트
- `HumanMessage` - 사용자 입력
- `AIMessage` - AI 응답
- `ToolMessage` - 도구 실행 결과
- Content Blocks - 멀티모달 콘텐츠

**실행 방법**:
```bash
python 02_messages.py
```

**주요 개념**:
- 메시지 리스트로 대화 구성
- 역할 기반 메시지 구조
- 메시지 히스토리 관리

---

### 03_tools_basic.py
**난이도**: ⭐⭐☆☆☆ | **예상 시간**: 30분

기본 도구를 정의하고 사용하는 방법을 학습합니다.

**학습 내용**:
- `@tool` 데코레이터 사용
- 도구 함수 작성 (docstring 중요!)
- 타입 힌트와 스키마 정의
- 기본 도구 테스트

**실행 방법**:
```bash
python 03_tools_basic.py
```

**주요 개념**:
- 도구는 LLM이 호출할 수 있는 함수
- Docstring이 LLM에게 전달되는 설명
- 타입 힌트로 파라미터 정의

---

### 04_tools_advanced.py
**난이도**: ⭐⭐⭐☆☆ | **예상 시간**: 40분

Pydantic 스키마를 사용한 고급 도구 패턴을 학습합니다.

**학습 내용**:
- Pydantic 모델로 복잡한 파라미터 정의
- 중첩된 스키마 사용
- 유효성 검증 (Validation)
- 선택적 파라미터 및 기본값

**실행 방법**:
```bash
python 04_tools_advanced.py
```

**주요 개념**:
- Pydantic의 타입 안정성
- 자동 스키마 생성
- 런타임 유효성 검증

---

### 05_tool_calling.py
**난이도**: ⭐⭐⭐☆☆ | **예상 시간**: 45분

LLM의 Tool Calling 메커니즘을 이해하고 활용합니다.

**학습 내용**:
- `bind_tools()` 메서드
- Tool Call 응답 파싱
- 도구 실행 및 결과 반환
- Tool Calling 루프 구현

**실행 방법**:
```bash
python 05_tool_calling.py
```

**주요 개념**:
- LLM이 언제 도구를 호출하는지
- Tool Call 파라미터 추출
- 도구 실행 후 결과를 LLM에 전달

---

## 🎓 실습 과제

### 과제 1: 계산기 도구 만들기 (⭐⭐☆)

**목표**: 사칙연산을 수행하는 도구를 만들어보세요.

**요구사항**:
1. `add`, `subtract`, `multiply`, `divide` 4개 도구 구현
2. 각 도구는 두 개의 숫자를 받아 연산 수행
3. divide는 0으로 나누기 방지

**힌트**:
```python
@tool
def add(a: float, b: float) -> float:
    """두 숫자를 더합니다."""
    return a + b
```

**해답**: [solutions/exercise_01.py](/src/part02_fundamentals/solutions/exercise_01.py)

---

### 과제 2: 날씨 조회 도구 (⭐⭐⭐)

**목표**: 도시 이름을 입력받아 날씨 정보를 반환하는 도구를 만드세요.

**요구사항**:
1. Pydantic 모델로 파라미터 정의
2. 도시, 단위(섭씨/화씨) 파라미터
3. Mock 데이터 또는 실제 API 연동 (선택)

**해답**: [solutions/exercise_02.py](/src/part02_fundamentals/solutions/exercise_02.py)

---

### 과제 3: Tool Calling 시스템 (⭐⭐⭐⭐)

**목표**: 여러 도구를 가진 간단한 Agent를 만들어보세요.

**요구사항**:
1. 3개 이상의 도구 정의
2. LLM에게 도구 바인딩
3. Tool Call 감지 및 실행
4. 최종 응답 생성

**해답**: [solutions/exercise_03.py](/src/part02_fundamentals/solutions/exercise_03.py)

---

## 💡 실전 팁

### Tip 1: 도구 설명 작성 요령
```python
@tool
def search_database(query: str, limit: int = 10) -> list:
    """
    데이터베이스를 검색합니다.

    Args:
        query: 검색할 키워드 (필수)
        limit: 반환할 최대 결과 수 (기본값: 10)

    Returns:
        검색 결과 리스트
    """
    # 구현...
```
- **명확한 설명**: LLM이 언제 이 도구를 사용해야 하는지 알려줍니다
- **파라미터 설명**: 각 파라미터의 의미와 형식을 명시합니다
- **예시 포함**: 가능하면 사용 예시를 docstring에 포함합니다

### Tip 2: 모델 선택 가이드
- **GPT-4o-mini**: 빠르고 저렴, 간단한 작업에 적합
- **GPT-4o**: 복잡한 추론, 정확도 중요한 경우
- **Claude 3.5 Sonnet**: 긴 컨텍스트, 복잡한 지시사항
- **Claude 3.5 Haiku**: 빠른 응답, 비용 효율적

### Tip 3: 에러 핸들링
```python
from langchain_core.tools import tool

@tool
def safe_divide(a: float, b: float) -> float:
    """안전하게 나눗셈을 수행합니다."""
    if b == 0:
        raise ValueError("0으로 나눌 수 없습니다")
    return a / b
```

---

## ❓ 자주 묻는 질문

<details>
<summary>Q1: LLM이 도구를 호출하지 않아요</summary>

**A**: 다음을 확인하세요:
1. **도구 설명 개선**: Docstring을 더 명확하게 작성
2. **프롬프트 수정**: "Use the available tools" 등의 명시적 지시
3. **모델 확인**: 일부 모델은 tool calling을 지원하지 않음
4. **도구 바인딩**: `bind_tools()`를 호출했는지 확인

```python
# 명시적 지시 예시
model = ChatOpenAI(model="gpt-4o-mini")
model_with_tools = model.bind_tools([my_tool])
```
</details>

<details>
<summary>Q2: Pydantic 모델과 @tool 중 어떤 것을 사용해야 하나요?</summary>

**A**:
- **간단한 도구** (1-3개 파라미터): `@tool` 데코레이터
- **복잡한 도구** (4개 이상, 중첩 구조): Pydantic 모델
- **유효성 검증 필요**: Pydantic 모델

```python
# 간단한 경우
@tool
def simple_tool(x: int) -> int:
    """간단한 도구"""
    return x * 2

# 복잡한 경우
class ComplexInput(BaseModel):
    name: str
    age: int
    address: dict

@tool(args_schema=ComplexInput)
def complex_tool(input: ComplexInput) -> str:
    """복잡한 도구"""
    return f"{input.name} is {input.age}"
```
</details>

<details>
<summary>Q3: Streaming 응답은 언제 사용하나요?</summary>

**A**: Streaming은 다음 상황에서 유용합니다:
- 긴 응답을 생성하는 경우
- 사용자에게 즉각적인 피드백을 주고 싶을 때
- 프론트엔드에서 타이핑 애니메이션을 구현할 때

```python
for chunk in model.stream([HumanMessage(content="긴 이야기를 들려줘")]):
    print(chunk.content, end="", flush=True)
```
</details>

---

## 🔗 심화 학습

이 파트의 기초를 마스터했다면:

1. **공식 문서 심화**
   - [07-models.md](/official/07-models.md) - Chat Models 전체 가이드
   - [08-messages.md](/official/08-messages.md) - 메시지 타입 상세
   - [09-tools.md](/official/09-tools.md) - 도구 고급 패턴

2. **관련 논문**
   - [Toolformer: Language Models Can Teach Themselves to Use Tools](https://arxiv.org/abs/2302.04761)
   - [Function Calling and Other API Updates](https://openai.com/index/function-calling-and-other-api-updates/)

3. **커뮤니티 리소스**
   - [LangChain Tool Use 가이드](https://python.langchain.com/docs/how_to/tool_calling/)
   - [프로바이더별 모델 비교](https://python.langchain.com/docs/integrations/chat/)

4. **다음 단계**
   - [Part 3: First Agent](/src/part03_first_agent/README.md) - 첫 Agent 만들기

---

## ✅ 체크리스트

Part 2를 완료하기 전에 확인하세요:

- [ ] 모든 예제 코드를 실행해봤다 (5개)
- [ ] 실습 과제를 완료했다 (3개)
- [ ] Chat Model의 기본 사용법을 이해했다
- [ ] 메시지 타입의 차이를 설명할 수 있다
- [ ] 기본 도구를 정의하고 사용할 수 있다
- [ ] Tool Calling 메커니즘을 이해했다

---

**이전**: [← Part 1 - Introduction](/src/part01_introduction/README.md)
**다음**: [Part 3 - First Agent로 이동](/src/part03_first_agent/README.md) →

---

**학습 진도**: ▓▓░░░░░░░░ 20% (Part 2/10 완료)

*마지막 업데이트: 2025-02-06*
