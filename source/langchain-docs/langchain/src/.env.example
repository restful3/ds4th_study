# ============================================================================
# LangChain AI Agent 마스터 교안 - 환경변수 설정
# ============================================================================
# 이 파일을 복사하여 .env 파일을 생성하세요:
#   cp .env.example .env
#
# 그 후 각 API 키를 실제 값으로 교체하세요.
# ============================================================================

# ----------------------------------------------------------------------------
# LLM 프로바이더 (최소 하나 필수)
# ----------------------------------------------------------------------------

# OpenAI (GPT-4o, GPT-4o-mini)
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=sk-proj-your-key-here

# Anthropic (Claude 3.5 Sonnet, Claude 3 Haiku)
# https://console.anthropic.com/ 에서 발급
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Google (Gemini)
# https://aistudio.google.com/apikey 에서 발급
GOOGLE_API_KEY=your-key-here

# ----------------------------------------------------------------------------
# LangSmith (Part 10에서 사용, 선택 사항)
# ----------------------------------------------------------------------------

# https://smith.langchain.com/ 에서 발급
LANGSMITH_API_KEY=lsv2_pt_your-key-here
LANGSMITH_TRACING=false

# ----------------------------------------------------------------------------
# 데이터베이스 (Part 4 이후, 선택 사항)
# ----------------------------------------------------------------------------

# PostgreSQL 연결 URL (Docker: docker run -d --name langchain-postgres -e POSTGRES_PASSWORD=password -p 5432:5432 postgres:15)
DATABASE_URL=postgresql://postgres:password@localhost:5432/langchain
